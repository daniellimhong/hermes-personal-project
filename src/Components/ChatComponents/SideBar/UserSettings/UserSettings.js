import React, { Component } from 'react';

export default class UserSettings extends Component {
    constructor(props){
        super(props);

        this.state = {
            user: {

            }
        }
    }


    //! Show menu for CRUD capabilities for editing Settings and stuff

    render(){
        return(
        <div>UserSettings Component</div>
        )
    }
}